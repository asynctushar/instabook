import { createSlice } from '@reduxjs/toolkit';

const initialState = {

}

const commentSlice = createSlice({
    name: 'comment',
    initialState,
    reducers: {
        
    }
});


export default commentSlice;